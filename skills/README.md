# Bundled plugin skills

These ship with the Claude Code plugin so the agent knows *when* to reach for
the toolkit's MCP tools.

Canonical source: the [skillet registry](https://github.com/jnMetaCode/skillet)
(`skills/` there) — edit them there first, then sync the copies here.
